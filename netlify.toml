[# Netlify configuration]
[build]
# Run from the client subdirectory
base = "client"
command = "npm run build"
publish = "dist"

[build.environment]
# Ensure a modern Node version for Vite 7
NODE_VERSION = "20"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200